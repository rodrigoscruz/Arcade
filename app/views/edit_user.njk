{% extends "layout_user.njk" %}
{% block pagina %}

<h1>Atualizar Usuário</h1>

<div class="col-md-6">
    <form action="/edit/{{usuario.id}}" method="post">
        <input type="hidden" name="_method" value="PUT">
        <div class="form-group">
            <label>Id</label>
            <p class="form-control" id="nome" placeholder="{{usuario.id}}"></p>
        </div>
        <div class="form-group">
            <label for="nome">Nome</label>
           <input type="text" class="form-control" name="nome" id="nome" placeholder="Siclano" value="{{usuario.nome}}">
        </div>
        <div class="form-group">
            <label for="email">Email</label>
           <input type="text" class="form-control" name="email" id="email" placeholder="siclano@mail.com" value="{{usuario.email}}">
        </div>
        <div class="form-group">
            <label for="senha">Senha</label>
            <input type="text" class="form-control" name="senha" id="senha" placeholder="siclano123" value="{{usuario.senha}}">
        </div>
        <button type="submit" class="btn btn-primary">Atualizar</button>
            <a href="/user" class="btn btn-default">Voltar</a>
        </form>
    </div>
    {% endblock %}