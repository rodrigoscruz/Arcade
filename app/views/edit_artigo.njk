{% extends "layout_artigo.njk" %}
{% block pagina %}

<h1>Atualizar Artigo</h1>

<div class="col-md-6">
    <form action="/editartigo/{{artigo.id}}" method="post">
        <input type="hidden" name="_method" value="PUT">
        
        <div class="form-group">
            <label for="id_criador">Nome do criador</label>
           <input type="text" class="form-control" name="id_criador" id="id_criador" placeholder="{{artigo.nome_criador}}" value="{{artigo.nome_criador}}">
        </div>
        
        <div class="form-group">
            <label for="estrelas">Data</label>
            <input type="text" class="form-control" name="data" id="data" placeholder="{{artigo.data}}" value="{{artigo.data}}">
        </div>

        <div class="form-group">
            <label for="estrelas">Categoria</label>
            <input type="text" class="form-control" name="categoria" id="categoria" placeholder="{{artigo.categoria}}" value="{{artigo.categoria}}">
        </div>

        <div class="form-group">
            <label for="estrelas">Título</label>
            <input type="text" class="form-control" name="titulo" id="titulo" placeholder="{{artigo.titulo}}" value="{{artigo.titulo}}">
        </div>

        <div class="form-group">
            <label for="estrelas">Corpo</label>
            <input type="text" class="form-control" name="corpo" id="corpo" placeholder="{{artigo.corpo}}" value="{{artigo.corpo}}">
        </div>

        <div class="form-group">
            <label for="estrelas">Imagem</label>
            <input type="text" class="form-control" name="img_url" id="img_url" placeholder="{{artigo.img_url}}" value="{{artigo.img_url}}">
        </div>


        <button type="submit" class="btn btn-primary">Atualizar</button>
            <a href="/artigo" class="btn btn-default">Voltar</a>
        </form>
    </div>
    {% endblock %}